---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: servers.m.cloudscale.ch
spec:
  group: m.cloudscale.ch
  names:
    categories:
    - crossplane
    - managed
    - cloudscale
    kind: Server
    listKind: ServerList
    plural: servers
    singular: server
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Server is the Schema for the Servers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServerSpec defines the desired state of Server
            properties:
              forProvider:
                properties:
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bulkVolumeSizeGb:
                    description: The size in GB of the bulk storage volume of the
                      new server. If this parameter is not specified, no bulk storage
                      volume will be attached to the server. Valid values are multiples
                      of 100.
                    type: number
                  flavorSlug:
                    description: |-
                      The slug (name) of the flavor to use for the new server. Possible values can be found in our API documentation.
                      Note: If you want to update this value after initial creation, you must set allow_stopping_for_update to true.
                    type: string
                  imageSlug:
                    description: The slug (name) of the image (or custom image) to
                      use for the new server. Possible values can be found in our
                      API documentation.
                    type: string
                  imageUuid:
                    description: 'The UUID of the custom image to use for the new
                      server. Note: This is the recommended approach for custom images.'
                    type: string
                  interfaces:
                    description: 'A list of interface configuration objects (see example).
                      Each interface object has the following attributes:'
                    items:
                      properties:
                        addresses:
                          description: 'A list of address objects:'
                          items:
                            properties:
                              address:
                                description: An IP address that has been assigned
                                  to this server.
                                type: string
                              subnetUuid:
                                description: The UUID of the subnet this address should
                                  be part of. Must be compatible with network_uuid
                                  if both are specified.
                                type: string
                              subnetUuidRef:
                                description: Reference to a Subnet in cloudscale to
                                  populate subnetUuid.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              subnetUuidSelector:
                                description: Selector for a Subnet in cloudscale to
                                  populate subnetUuid.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        networkUuid:
                          description: The UUID of the private network this interface
                            should be attached to. Must be compatible with subnet_uuid
                            if both are specified.
                          type: string
                        networkUuidRef:
                          description: Reference to a Network in cloudscale to populate
                            networkUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        networkUuidSelector:
                          description: Selector for a Network in cloudscale to populate
                            networkUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        noAddress:
                          description: You need to set this to true if no address
                            should be configured, e.g. if you want to attach to a
                            network without a subnet.
                          type: boolean
                        type:
                          description: The type of the interface. Can be public or
                            private.
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name of the new server. The name has to be a valid
                      host name or a fully qualified domain name (FQDN).
                    type: string
                  passwordSecretRef:
                    description: The password of the default user of the new server.
                      When omitted, no password will be set.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  serverGroupIds:
                    description: A list of server group UUIDs to which the server
                      should be added. Default to an empty list.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  skipWaitingForSshHostKeys:
                    description: If set to true, do not wait until SSH host keys become
                      available.
                    type: boolean
                  sshKeys:
                    description: A list of SSH public keys. Use the full content of
                      your *.pub file here.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  status:
                    description: The desired state of a server. Can be running (default)
                      or stopped.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  useIpv6:
                    description: Enable/disable IPv6 on the public network interface
                      of the new server. Can be true (default) or false.
                    type: boolean
                  usePrivateNetwork:
                    description: Attach the default private network interface to the
                      new server. Can be true or false (default). Use interfaces option
                      for advanced setups.
                    type: boolean
                  usePublicNetwork:
                    description: Attach the public network interface to the new server.
                      Can be true (default) or false. Use interfaces option for advanced
                      setups.
                    type: boolean
                  userData:
                    description: User data (custom cloud-config settings) to use for
                      the new server. Needs to be valid YAML. A default configuration
                      will be used if this parameter is not specified or set to null.
                      Use only if you are an advanced user with knowledge of cloud-config
                      and cloud-init.
                    type: string
                  volumeSizeGb:
                    description: The size in GB of the SSD root volume of the new
                      server. If this parameter is not specified, the value will be
                      set to 10. The minimum value is 10.
                    type: number
                  zoneSlug:
                    description: The slug of the zone in which the new server will
                      be created. Options include lpg1 and rma1.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bulkVolumeSizeGb:
                    description: The size in GB of the bulk storage volume of the
                      new server. If this parameter is not specified, no bulk storage
                      volume will be attached to the server. Valid values are multiples
                      of 100.
                    type: number
                  flavorSlug:
                    description: |-
                      The slug (name) of the flavor to use for the new server. Possible values can be found in our API documentation.
                      Note: If you want to update this value after initial creation, you must set allow_stopping_for_update to true.
                    type: string
                  imageSlug:
                    description: The slug (name) of the image (or custom image) to
                      use for the new server. Possible values can be found in our
                      API documentation.
                    type: string
                  imageUuid:
                    description: 'The UUID of the custom image to use for the new
                      server. Note: This is the recommended approach for custom images.'
                    type: string
                  interfaces:
                    description: 'A list of interface configuration objects (see example).
                      Each interface object has the following attributes:'
                    items:
                      properties:
                        addresses:
                          description: 'A list of address objects:'
                          items:
                            properties:
                              address:
                                description: An IP address that has been assigned
                                  to this server.
                                type: string
                              subnetUuid:
                                description: The UUID of the subnet this address should
                                  be part of. Must be compatible with network_uuid
                                  if both are specified.
                                type: string
                              subnetUuidRef:
                                description: Reference to a Subnet in cloudscale to
                                  populate subnetUuid.
                                properties:
                                  name:
                                    description: Name of the referenced object.
                                    type: string
                                  namespace:
                                    description: Namespace of the referenced object
                                    type: string
                                  policy:
                                    description: Policies for referencing.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                required:
                                - name
                                type: object
                              subnetUuidSelector:
                                description: Selector for a Subnet in cloudscale to
                                  populate subnetUuid.
                                properties:
                                  matchControllerRef:
                                    description: |-
                                      MatchControllerRef ensures an object with the same controller reference
                                      as the selecting object is selected.
                                    type: boolean
                                  matchLabels:
                                    additionalProperties:
                                      type: string
                                    description: MatchLabels ensures an object with
                                      matching labels is selected.
                                    type: object
                                  namespace:
                                    description: Namespace for the selector
                                    type: string
                                  policy:
                                    description: Policies for selection.
                                    properties:
                                      resolution:
                                        default: Required
                                        description: |-
                                          Resolution specifies whether resolution of this reference is required.
                                          The default is 'Required', which means the reconcile will fail if the
                                          reference cannot be resolved. 'Optional' means this reference will be
                                          a no-op if it cannot be resolved.
                                        enum:
                                        - Required
                                        - Optional
                                        type: string
                                      resolve:
                                        description: |-
                                          Resolve specifies when this reference should be resolved. The default
                                          is 'IfNotPresent', which will attempt to resolve the reference only when
                                          the corresponding field is not present. Use 'Always' to resolve the
                                          reference on every reconcile.
                                        enum:
                                        - Always
                                        - IfNotPresent
                                        type: string
                                    type: object
                                type: object
                            type: object
                          type: array
                        networkUuid:
                          description: The UUID of the private network this interface
                            should be attached to. Must be compatible with subnet_uuid
                            if both are specified.
                          type: string
                        networkUuidRef:
                          description: Reference to a Network in cloudscale to populate
                            networkUuid.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            namespace:
                              description: Namespace of the referenced object
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        networkUuidSelector:
                          description: Selector for a Network in cloudscale to populate
                            networkUuid.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            namespace:
                              description: Namespace for the selector
                              type: string
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        noAddress:
                          description: You need to set this to true if no address
                            should be configured, e.g. if you want to attach to a
                            network without a subnet.
                          type: boolean
                        type:
                          description: The type of the interface. Can be public or
                            private.
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name of the new server. The name has to be a valid
                      host name or a fully qualified domain name (FQDN).
                    type: string
                  passwordSecretRef:
                    description: The password of the default user of the new server.
                      When omitted, no password will be set.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  serverGroupIds:
                    description: A list of server group UUIDs to which the server
                      should be added. Default to an empty list.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  skipWaitingForSshHostKeys:
                    description: If set to true, do not wait until SSH host keys become
                      available.
                    type: boolean
                  sshKeys:
                    description: A list of SSH public keys. Use the full content of
                      your *.pub file here.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  status:
                    description: The desired state of a server. Can be running (default)
                      or stopped.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  useIpv6:
                    description: Enable/disable IPv6 on the public network interface
                      of the new server. Can be true (default) or false.
                    type: boolean
                  usePrivateNetwork:
                    description: Attach the default private network interface to the
                      new server. Can be true or false (default). Use interfaces option
                      for advanced setups.
                    type: boolean
                  usePublicNetwork:
                    description: Attach the public network interface to the new server.
                      Can be true (default) or false. Use interfaces option for advanced
                      setups.
                    type: boolean
                  userData:
                    description: User data (custom cloud-config settings) to use for
                      the new server. Needs to be valid YAML. A default configuration
                      will be used if this parameter is not specified or set to null.
                      Use only if you are an advanced user with knowledge of cloud-config
                      and cloud-init.
                    type: string
                  volumeSizeGb:
                    description: The size in GB of the SSD root volume of the new
                      server. If this parameter is not specified, the value will be
                      set to 10. The minimum value is 10.
                    type: number
                  zoneSlug:
                    description: The slug of the zone in which the new server will
                      be created. Options include lpg1 and rma1.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.flavorSlug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.flavorSlug)
                || (has(self.initProvider) && has(self.initProvider.flavorSlug))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ServerStatus defines the observed state of Server.
            properties:
              atProvider:
                properties:
                  allowStoppingForUpdate:
                    description: If you try to update a property that requires stopping
                      the instance without setting this field, the update will fail.
                    type: boolean
                  bulkVolumeSizeGb:
                    description: The size in GB of the bulk storage volume of the
                      new server. If this parameter is not specified, no bulk storage
                      volume will be attached to the server. Valid values are multiples
                      of 100.
                    type: number
                  flavorSlug:
                    description: |-
                      The slug (name) of the flavor to use for the new server. Possible values can be found in our API documentation.
                      Note: If you want to update this value after initial creation, you must set allow_stopping_for_update to true.
                    type: string
                  href:
                    description: The cloudscale.ch API URL of the current resource.
                    type: string
                  id:
                    description: The UUID of this server.
                    type: string
                  imageSlug:
                    description: The slug (name) of the image (or custom image) to
                      use for the new server. Possible values can be found in our
                      API documentation.
                    type: string
                  imageUuid:
                    description: 'The UUID of the custom image to use for the new
                      server. Note: This is the recommended approach for custom images.'
                    type: string
                  interfaces:
                    description: 'A list of interface configuration objects (see example).
                      Each interface object has the following attributes:'
                    items:
                      properties:
                        addresses:
                          description: 'A list of address objects:'
                          items:
                            properties:
                              address:
                                description: An IP address that has been assigned
                                  to this server.
                                type: string
                              gateway:
                                description: An IPv4 or IPv6 address that represents
                                  the default gateway for this interface.
                                type: string
                              prefixLength:
                                description: The prefix length for this IP address,
                                  typically 24 for IPv4 and 128 for IPv6.
                                type: number
                              reversePtr:
                                description: The PTR record (reverse DNS pointer)
                                  for this IP address. If you use an FQDN as your
                                  server name it will automatically be used here.
                                type: string
                              subnetCidr:
                                description: The cidr of the subnet the address is
                                  part of.
                                type: string
                              subnetHref:
                                description: The cloudscale.ch API URL of the subnet
                                  the address is part of.
                                type: string
                              subnetUuid:
                                description: The UUID of the subnet this address should
                                  be part of. Must be compatible with network_uuid
                                  if both are specified.
                                type: string
                              version:
                                description: The IP version, either 4 or 6.
                                type: number
                            type: object
                          type: array
                        networkHref:
                          description: The cloudscale.ch API URL of the network the
                            interface is attached to.
                          type: string
                        networkName:
                          description: The name of the network the interface is attached
                            to.
                          type: string
                        networkUuid:
                          description: The UUID of the private network this interface
                            should be attached to. Must be compatible with subnet_uuid
                            if both are specified.
                          type: string
                        noAddress:
                          description: You need to set this to true if no address
                            should be configured, e.g. if you want to attach to a
                            network without a subnet.
                          type: boolean
                        type:
                          description: The type of the interface. Can be public or
                            private.
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name of the new server. The name has to be a valid
                      host name or a fully qualified domain name (FQDN).
                    type: string
                  privateIpv4Address:
                    description: The first private IPv4 address of this server. The
                      returned IP address may be "" if the server does not have private
                      networking enabled.
                    type: string
                  publicIpv4Address:
                    description: The first public IPv4 address of this server. The
                      returned IP address may be "" if the server does not have a
                      public IPv4.
                    type: string
                  publicIpv6Address:
                    description: The first public IPv6 address of this server. The
                      returned IP address may be "" if the server does not have a
                      public IPv6.
                    type: string
                  serverGroupIds:
                    description: A list of server group UUIDs to which the server
                      should be added. Default to an empty list.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  serverGroups:
                    items:
                      properties:
                        href:
                          description: The cloudscale.ch API URL of the current resource.
                          type: string
                        name:
                          description: Name of the new server. The name has to be
                            a valid host name or a fully qualified domain name (FQDN).
                          type: string
                        uuid:
                          description: The UUID of this server.
                          type: string
                      type: object
                    type: array
                  skipWaitingForSshHostKeys:
                    description: If set to true, do not wait until SSH host keys become
                      available.
                    type: boolean
                  sshFingerprints:
                    description: A list of SSH host key fingerprints (strings) of
                      this server.
                    items:
                      type: string
                    type: array
                  sshHostKeys:
                    description: A list of SSH host keys (strings) of this server.
                    items:
                      type: string
                    type: array
                  sshKeys:
                    description: A list of SSH public keys. Use the full content of
                      your *.pub file here.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  status:
                    description: The desired state of a server. Can be running (default)
                      or stopped.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: |-
                      Tags allow you to assign custom metadata to resources:
                      tags = {
                      foo = "bar"
                      }

                      Tags are always strings (both keys and values).
                    type: object
                    x-kubernetes-map-type: granular
                  useIpv6:
                    description: Enable/disable IPv6 on the public network interface
                      of the new server. Can be true (default) or false.
                    type: boolean
                  usePrivateNetwork:
                    description: Attach the default private network interface to the
                      new server. Can be true or false (default). Use interfaces option
                      for advanced setups.
                    type: boolean
                  usePublicNetwork:
                    description: Attach the public network interface to the new server.
                      Can be true (default) or false. Use interfaces option for advanced
                      setups.
                    type: boolean
                  userData:
                    description: User data (custom cloud-config settings) to use for
                      the new server. Needs to be valid YAML. A default configuration
                      will be used if this parameter is not specified or set to null.
                      Use only if you are an advanced user with knowledge of cloud-config
                      and cloud-init.
                    type: string
                  volumeSizeGb:
                    description: The size in GB of the SSD root volume of the new
                      server. If this parameter is not specified, the value will be
                      set to 10. The minimum value is 10.
                    type: number
                  volumes:
                    description: 'A list of volume objects attached to this server.
                      Each volume object has three attributes:'
                    items:
                      properties:
                        devicePath:
                          description: The path (string) to the volume on your server
                            (e.g. /dev/vda)
                          type: string
                        sizeGb:
                          description: The size (int) of the volume in GB. Typically
                            matches volume_size_gb or bulk_volume_size_gb.
                          type: number
                        type:
                          description: The type of the interface. Can be public or
                            private.
                          type: string
                        uuid:
                          description: The UUID of this server.
                          type: string
                      type: object
                    type: array
                  zoneSlug:
                    description: The slug of the zone in which the new server will
                      be created. Options include lpg1 and rma1.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
